doctype
html
	head
		meta(charset="utf-8")
		meta(http-equiv="X-UA-Compatible" content="IE=edge")
		meta(name="viewport" content="width=device-width, initial-scale=1")
		meta(name="description" content="")
		meta(name="author" content="Gaoping Huang")
		link(rel="icon" href="")
		block styles

		title #{title}
		include ./partials/head
	body
		block content
		footer Copyright &copy; 2016
		
		script
			if amt_hit
				| // Global variables
				| var Helper = window.Helper = window.Helper || {};
				| Helper.task_id = #{amt_hit.task_id};
				| Helper.worker_id = "#{amt_hit.worker_id}";
		block scripts